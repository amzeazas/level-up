<% if user_signed_in? && current_user.admin? %>
  <h1>All Users:</h1>
  <% @users.each do |user| %>
  <ul>
    <li><%= link_to "#{user.username}", user_path(user) %></li>
  </ul>
  <% end %>
<% end %>
