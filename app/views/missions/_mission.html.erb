<%= form_for mission, remote: true do |f| %>
  <%= f.check_box :complete %>
  <%= f.submit "Update" %>
  <%= f.label :complete, mission.description %>
  <%= link_to "(remove)", mission, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>
